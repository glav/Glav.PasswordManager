<Window x:Class="PasswordMgr.PeekWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PeekWindow" Height="100" Width="400" BorderBrush="Transparent" WindowStyle="None">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="FontSize" Value="18.667"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <StackPanel x:Name="peekPassword" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!--<TextBlock Text="test stuff"></TextBlock>-->
        </StackPanel>
    </Grid>
</Window>
